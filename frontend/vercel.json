{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "CI=false NODE_OPTIONS=--openssl-legacy-provider npm run build",
  "outputDirectory": "build",
  "rewrites": [{ "source": "/((?!api/).*)", "destination": "/index.html" }],
  "env": {
    "PUBLIC_URL": ""
  }
}
